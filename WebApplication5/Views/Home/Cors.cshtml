@{
    ViewData["Title"] = "Cors";
}

<h1>Cors</h1>

@section Scripts {
    <script>
        (async () => {
            const response = await fetch(`https://localhost:7113/api/Categories`);
            const datas = await response.json();
            console.log(datas);
        })()
    </script>
}